import { keyframes } from 'styled-components';

const transformShadowAnimation = keyframes`
  from {
    box-shadow: 25px 25px 0 #9cf0c7;
  }
  to {
    box-shadow: 0 0 0 #9cf0c7;
  }
`;

export default transformShadowAnimation;
